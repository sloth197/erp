# ERP Desktop

## Initial Seed Accounts

- `admin` / `${ERP_SEED_ADMIN_PASSWORD}` (change password after first login)
- `staff` / `${ERP_SEED_STAFF_PASSWORD}`

## Database

- PostgreSQL connection: `Host=localhost;Port=5432;Database=${ERP_DB_NAME};Username=${ERP_DB_USER};Password=${ERP_DB_PASSWORD}`


## Security Notes

- This repository does not store real credentials.
