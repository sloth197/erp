{
  "ConnectionStrings": {
    "ErpDb": "Host=localhost;Port=5432;Database=${ERP_DB_NAME};Username=${ERP_DB_USER};Password=${ERP_DB_PASSWORD};Include Error Detail=true"
  },
  "Seed": {
    "AdminUsername": "admin",
    "AdminPassword": "${ERP_SEED_ADMIN_PASSWORD}",
    "StaffUsername": "staff",
    "StaffPassword": "${ERP_SEED_STAFF_PASSWORD}"
  }
}
