<Window x:Class="Erp.Desktop.Views.AddressSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="주소 검색"
        Width="840"
        Height="620"
        MinWidth="700"
        MinHeight="500"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0"
                   Margin="0,0,0,10">
            <TextBox x:Name="KeywordTextBox"
                     Width="560"
                     Height="34"
                     VerticalContentAlignment="Center"
                     Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}" />
            <Button DockPanel.Dock="Right"
                    Width="82"
                    Height="34"
                    Margin="8,0,0,0"
                    Content="검색"
                    Command="{Binding SearchCommand}" />
        </DockPanel>

        <TextBlock Grid.Row="1"
                   Margin="0,0,0,10"
                   Foreground="#667085"
                   Text="{Binding StatusText}" />

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Results}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  RowHeaderWidth="0"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  BorderBrush="#D0D7E2"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="도로명 주소"
                                    Binding="{Binding RoadAddress}"
                                    Width="3*" />
                <DataGridTextColumn Header="지번 주소"
                                    Binding="{Binding JibunAddress}"
                                    Width="3*" />
                <DataGridTextColumn Header="우편번호"
                                    Binding="{Binding ZipNo}"
                                    Width="120" />
                <DataGridTemplateColumn Header=""
                                        Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Width="62"
                                    Height="28"
                                    Content="선택"
                                    Click="SelectButton_OnClick" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Margin="0,10,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Width="80"
                    Height="30"
                    Content="닫기"
                    Click="CloseButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
